<div class="cloud">
    {% for tag in tags %}
        <div data-tag-name = "{{ tag.tag }}" class="tagDraggable"><a style="font-size: {{ tag.size }}px" href="{{ path('show_tag_rss', {'name' : tag.tag}) }}">{{ tag.tag }}</a>{% if tag != tags|last %},&nbsp;{% endif %}</div>
    {% endfor %}
</div>
